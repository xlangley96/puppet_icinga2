[general]
timezone = "UTC"

[authentication]
backend = "db"

[database]
type = "mysql"
host = "<%= @db_host %>"
dbname = "<%= @db_name %>"
username = "<%= @db_user %>"
password = "<%= @db_password %>"
